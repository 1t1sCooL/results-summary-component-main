apiVersion: v1
kind: Service
metadata:
  name: results-summary-component-main
  namespace: default
spec:
  selector:
    app: results-summary-component-main-app
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
