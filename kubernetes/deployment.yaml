apiVersion: apps/v1
kind: Deployment
metadata:
  name: results-summary-component-main-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: results-summary-component-main-app
  template:
    metadata:
      labels:
        app: results-summary-component-main-app
    spec:
      containers:
        - name: results-summary-component-main-app
          image: results-summary-component-main-app:latest
          ports:
            - containerPort: 80
          env:
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
