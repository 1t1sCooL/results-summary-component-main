apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: results-summary-component-main
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
    - host: mmalabugin.ru
      http:
        paths:
          - path: /ResultsSummaryComponent
            pathType: Prefix
            backend:
              service:
                name: results-summary-component-main
                port:
                  number: 80
  tls:
    - hosts:
        - mmalabugin.ru
      secretName: main-page-tls
